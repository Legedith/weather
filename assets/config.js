// Public configuration only. NEVER put a Google API key in this file.
// Set GOOGLE_WEATHER_PROXY_URL as a GitHub Actions repository variable, or
// replace the empty string below with your deployed HTTPS /forecast endpoint.
export const config = Object.freeze({ googleWeatherProxyUrl: '' });
